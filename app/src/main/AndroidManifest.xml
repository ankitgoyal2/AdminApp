<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowDetailsActivity"></activity>
        <activity android:name=".GetMachineDetailsActivity" />
        <activity android:name=".StockRecordActivity" />
        <activity android:name=".MechanicListActivity" />
        <activity android:name=".ManagerListActivity" />
        <activity android:name=".MechanicProfileActivity" />
        <activity android:name=".ManagerProfileActivity" />
        <activity android:name=".GenerateQRActivity" />
        <activity android:name=".AddEmployeeActivity" />
        <activity android:name=".BroadcastActivity" />
        <activity
            android:name=".BottomNavigationActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".UsersActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>